# next-auth v5 beta declares an optional peer on nodemailer ^7||^8 while shipping
# nodemailer 9. bun (upstream's package manager) tolerates that; npm does not, and
# Vercel installs with npm because we committed package-lock.json. Without this the
# build dies on "npm install exited with 1".
legacy-peer-deps=true
